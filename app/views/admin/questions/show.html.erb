<p>
  <strong>Topic:</strong>
  <%= @question.topic.title %>
</p>

<p>
  <strong>Question:</strong>
  <%= @question.content %>
</p>

<%= link_to 'Edit Question', edit_admin_topic_question_path(@topic, @question), { class: 'ma-link' }%>
<%= link_to 'Back', admin_topic_questions_path(@topic), { class: 'ma-link' } %>

<br />
<br />

<%= render partial: 'responses', locals: { responses: @question.responses } %>

<%= render partial: 'directions', locals: { directions: @question.directions } %>

<div>
  <p>transition</p>
</div>

<div>
  <p>topic end location</p>
</div>
