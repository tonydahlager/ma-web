<%= form_for([:admin, @topic, @bridge]) do |f| %>
  <% if @bridge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bridge.errors.count, "error") %> prohibited this bridge from being saved:</h2>

      <ul>
      <% @bridge.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :content %><br>
    <%= f.text_area :content, class: 'ma-text-edit' %>
  </div>
  
  <div class="actions">
    <%= f.submit class: 'btn btn-default' %>
  </div>
<% end %>
